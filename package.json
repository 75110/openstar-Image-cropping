{
  "name": "batch-image-splitter",
  "version": "1.0.0",
  "scripts": {
    "dev": "cargo run",
    "watch": "cargo watch -x run",
    "build": "cargo build --release",
    "check": "cargo check"
  }
}
